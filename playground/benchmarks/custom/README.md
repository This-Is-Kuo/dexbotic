# Custom Benchmarks

Place user-maintained custom benchmark experiment files in this directory.

Notes:

- `__pycache__/` is ignored globally, so if this directory only contains bytecode cache files,
  Git may display the directory as ignored.
- Keeping this README ensures the directory itself remains visible and trackable.
